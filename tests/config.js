/**
 *  Adjust sinon to throw js Test Driver native Exceptions.
 *
 */
//sinon.assert.fail = function(){};
//var assertUndefined = function assertUndefined()
/*
assertContains = function(list, element) {
	return assertTrue("Element " + element + " is supposed to be in list " + list + ", but was not.", _.contains(list, element));
};
assertNotContains = function(list, element) {
	return assertFalse("Element " + element + " is not supposed to be in list " + list + ", but it was.", !(_.contains(list, element)));
};
*/